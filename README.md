
 # Tenant AI Copilot Design Prompt

 This is a code bundle for Tenant AI Copilot Design Prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  